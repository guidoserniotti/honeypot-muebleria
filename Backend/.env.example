# ⚠️ CONFIGURACIÓN VULNERABLE - SOLO PARA HONEYPOT ⚠️

# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=vulnerable123
DB_NAME=honeypot_db

# JWT Secret (INTENCIONALMENTE DÉBIL)
JWT_SECRET=insecure-secret-key-123

# Backdoor de desarrollo
DEV_BYPASS_HEADER=X-AccessDev
DEV_BYPASS_VALUE=Testing-Mode

# CORS
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=debug
LOG_SUSPICIOUS_ACTIVITY=true
